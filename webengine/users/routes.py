from flask import Blueprint

users = Blueprint('users', __name__)


# import threading
# from subprocess import call
# def thread_second():
#         call(["python3", "WebAPItoDB.py"])
#     processThread = threading.Thread(target=thread_second)
#     processThread.start()
